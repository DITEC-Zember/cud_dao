package sk.ditec.dao.master;

import java.math.BigDecimal;
import java.sql.Connection;
import java.sql.SQLException;
import java.util.ArrayList;
import java.util.Date;
import java.util.Iterator;
import java.util.LinkedList;
import java.util.List;

import org.apache.torque.NoRowsException;
import org.apache.torque.TooManyRowsException;
import org.apache.torque.Torque;
import org.apache.torque.TorqueException;
import org.apache.torque.map.MapBuilder;
import org.apache.torque.map.TableMap;
import org.apache.torque.om.DateKey;
import org.apache.torque.om.NumberKey;
import org.apache.torque.om.StringKey;
import org.apache.torque.om.ObjectKey;
import org.apache.torque.om.SimpleKey;
import org.apache.torque.util.BasePeer;
import org.apache.torque.util.Criteria;

import com.workingdogs.village.DataSetException;
import com.workingdogs.village.QueryDataSet;
import com.workingdogs.village.Record;

// Local classes
import sk.ditec.dao.master.map.*;



/**
 * This class was autogenerated by Torque on:
 *
 * [Fri Mar 20 13:51:25 CET 2026]
 *
 */
public abstract class BaseTCompanyPeer
    extends BasePeer
{
    /** Serial version */
    private static final long serialVersionUID = 1774011085524L;


    /** the default database name for this class */
    public static final String DATABASE_NAME;

     /** the table name for this class */
    public static final String TABLE_NAME;

    /**
     * @return the map builder for this peer
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static MapBuilder getMapBuilder()
        throws TorqueException
    {
        return getMapBuilder(TCompanyMapBuilder.CLASS_NAME);
    }

      /** the column name for the HIST_ID field */
    public static final String HIST_ID;
      /** the column name for the PLATNOST_OD field */
    public static final String PLATNOST_OD;
      /** the column name for the PLATNOST_DO field */
    public static final String PLATNOST_DO;
      /** the column name for the CAS_VYTVORENIA field */
    public static final String CAS_VYTVORENIA;
      /** the column name for the CAS_ZMENY field */
    public static final String CAS_ZMENY;
      /** the column name for the ID_ZMENA field */
    public static final String ID_ZMENA;
      /** the column name for the ZMAZ field */
    public static final String ZMAZ;
      /** the column name for the COMPANY_ID field */
    public static final String COMPANY_ID;
      /** the column name for the ID_COUNTRY field */
    public static final String ID_COUNTRY;
      /** the column name for the COMPANY_NAME field */
    public static final String COMPANY_NAME;
      /** the column name for the COMPANY_NAME_ASCII field */
    public static final String COMPANY_NAME_ASCII;
      /** the column name for the COMPANY_UIC_CODE field */
    public static final String COMPANY_UIC_CODE;
      /** the column name for the COMPANY_URL field */
    public static final String COMPANY_URL;
      /** the column name for the START_VALIDITY field */
    public static final String START_VALIDITY;
      /** the column name for the END_VALIDITY field */
    public static final String END_VALIDITY;
      /** the column name for the COMPANY_SHORT_NAME field */
    public static final String COMPANY_SHORT_NAME;
      /** the column name for the FREE_TEXT field */
    public static final String FREE_TEXT;
      /** the column name for the FREIGHT_FLAG field */
    public static final String FREIGHT_FLAG;
      /** the column name for the PASSENGER_FLAG field */
    public static final String PASSENGER_FLAG;
      /** the column name for the INFRASTRUCTURE_FLAG field */
    public static final String INFRASTRUCTURE_FLAG;
      /** the column name for the OTHER_COMPANY_FLAG field */
    public static final String OTHER_COMPANY_FLAG;
      /** the column name for the NE_ENTITY_FLAG field */
    public static final String NE_ENTITY_FLAG;
      /** the column name for the CE_ENTITY_FLAG field */
    public static final String CE_ENTITY_FLAG;
      /** the column name for the CONTACT_PERSON field */
    public static final String CONTACT_PERSON;
      /** the column name for the EMAIL field */
    public static final String EMAIL;
      /** the column name for the PHONE_NUMBER field */
    public static final String PHONE_NUMBER;
      /** the column name for the FAX_NUMBER field */
    public static final String FAX_NUMBER;
      /** the column name for the ADDRESS field */
    public static final String ADDRESS;
      /** the column name for the CITY field */
    public static final String CITY;
      /** the column name for the MOBILE_NUMBER field */
    public static final String MOBILE_NUMBER;
      /** the column name for the POSTAL_CODE field */
    public static final String POSTAL_CODE;
      /** the column name for the ACTIVE_FLAG field */
    public static final String ACTIVE_FLAG;
  
    static
    {
        DATABASE_NAME = "APP";
        TABLE_NAME = "T_COMPANY";

              HIST_ID = "T_COMPANY.HIST_ID";
                  PLATNOST_OD = "T_COMPANY.PLATNOST_OD";
                  PLATNOST_DO = "T_COMPANY.PLATNOST_DO";
                  CAS_VYTVORENIA = "T_COMPANY.CAS_VYTVORENIA";
                  CAS_ZMENY = "T_COMPANY.CAS_ZMENY";
                  ID_ZMENA = "T_COMPANY.ID_ZMENA";
                  ZMAZ = "T_COMPANY.ZMAZ";
                  COMPANY_ID = "T_COMPANY.COMPANY_ID";
                  ID_COUNTRY = "T_COMPANY.ID_COUNTRY";
                  COMPANY_NAME = "T_COMPANY.COMPANY_NAME";
                  COMPANY_NAME_ASCII = "T_COMPANY.COMPANY_NAME_ASCII";
                  COMPANY_UIC_CODE = "T_COMPANY.COMPANY_UIC_CODE";
                  COMPANY_URL = "T_COMPANY.COMPANY_URL";
                  START_VALIDITY = "T_COMPANY.START_VALIDITY";
                  END_VALIDITY = "T_COMPANY.END_VALIDITY";
                  COMPANY_SHORT_NAME = "T_COMPANY.COMPANY_SHORT_NAME";
                  FREE_TEXT = "T_COMPANY.FREE_TEXT";
                  FREIGHT_FLAG = "T_COMPANY.FREIGHT_FLAG";
                  PASSENGER_FLAG = "T_COMPANY.PASSENGER_FLAG";
                  INFRASTRUCTURE_FLAG = "T_COMPANY.INFRASTRUCTURE_FLAG";
                  OTHER_COMPANY_FLAG = "T_COMPANY.OTHER_COMPANY_FLAG";
                  NE_ENTITY_FLAG = "T_COMPANY.NE_ENTITY_FLAG";
                  CE_ENTITY_FLAG = "T_COMPANY.CE_ENTITY_FLAG";
                  CONTACT_PERSON = "T_COMPANY.CONTACT_PERSON";
                  EMAIL = "T_COMPANY.EMAIL";
                  PHONE_NUMBER = "T_COMPANY.PHONE_NUMBER";
                  FAX_NUMBER = "T_COMPANY.FAX_NUMBER";
                  ADDRESS = "T_COMPANY.ADDRESS";
                  CITY = "T_COMPANY.CITY";
                  MOBILE_NUMBER = "T_COMPANY.MOBILE_NUMBER";
                  POSTAL_CODE = "T_COMPANY.POSTAL_CODE";
                  ACTIVE_FLAG = "T_COMPANY.ACTIVE_FLAG";
              if (Torque.isInit())
        {
            try
            {
                getMapBuilder(TCompanyMapBuilder.CLASS_NAME);
            }
            catch (Exception e)
            {
                log.error("Could not initialize Peer", e);
                throw new RuntimeException(e);
            }
        }
        else
        {
            Torque.registerMapBuilder(TCompanyMapBuilder.CLASS_NAME);
        }
    }
 
    /** number of columns for this peer */
    public static final int numColumns =  32;

    /** A class that can be returned by this peer. */
    protected static final String CLASSNAME_DEFAULT =
        "sk.ditec.dao.master.TCompany";

    /** A class that can be returned by this peer. */
    protected static final Class CLASS_DEFAULT = initClass(CLASSNAME_DEFAULT);

    /**
     * Class object initialization method.
     *
     * @param className name of the class to initialize
     * @return the initialized class
     */
    private static Class initClass(String className)
    {
        Class c = null;
        try
        {
            c = Class.forName(className);
        }
        catch (Throwable t)
        {
            log.error("A FATAL ERROR has occurred which should not "
                + "have happened under any circumstance.  Please notify "
                + "the Torque developers <torque-dev@db.apache.org> "
                + "and give as many details as possible (including the error "
                + "stack trace).", t);

            // Error objects should always be propogated.
            if (t instanceof Error)
            {
                throw (Error) t.fillInStackTrace();
            }
        }
        return c;
    }

    /**
     * Get the list of objects for a ResultSet.  Please not that your
     * resultset MUST return columns in the right order.  You can use
     * getFieldNames() in BaseObject to get the correct sequence.
     *
     * @param results the ResultSet
     * @return the list of objects
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static List resultSet2Objects(java.sql.ResultSet results)
            throws TorqueException
    {
        try
        {
            QueryDataSet qds = null;
            List rows = null;
            try
            {
                qds = new QueryDataSet(results);
                rows = getSelectResults(qds);
            }
            finally
            {
                if (qds != null)
                {
                    qds.close();
                }
            }

            return populateObjects(rows);
        }
        catch (SQLException e)
        {
            throw new TorqueException(e);
        }
        catch (DataSetException e)
        {
            throw new TorqueException(e);
        }
    }


  
    /**
     * Method to do inserts.
     *
     * @param criteria object used to create the INSERT statement.
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static ObjectKey doInsert(Criteria criteria)
        throws TorqueException
    {
        return BaseTCompanyPeer
            .doInsert(criteria, (Connection) null);
    }

    /**
     * Method to do inserts.  This method is to be used during a transaction,
     * otherwise use the doInsert(Criteria) method.  It will take care of
     * the connection details internally.
     *
     * @param criteria object used to create the INSERT statement.
     * @param con the connection to use
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static ObjectKey doInsert(Criteria criteria, Connection con)
        throws TorqueException
    {
        correctBooleans(criteria);

        setDbName(criteria);

        if (con == null)
        {
            return BasePeer.doInsert(criteria);
        }
        else
        {
            return BasePeer.doInsert(criteria, con);
        }
    }

    /**
     * Add all the columns needed to create a new object.
     *
     * @param criteria object containing the columns to add.
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void addSelectColumns(Criteria criteria)
            throws TorqueException
    {
          criteria.addSelectColumn(HIST_ID);
          criteria.addSelectColumn(PLATNOST_OD);
          criteria.addSelectColumn(PLATNOST_DO);
          criteria.addSelectColumn(CAS_VYTVORENIA);
          criteria.addSelectColumn(CAS_ZMENY);
          criteria.addSelectColumn(ID_ZMENA);
          criteria.addSelectColumn(ZMAZ);
          criteria.addSelectColumn(COMPANY_ID);
          criteria.addSelectColumn(ID_COUNTRY);
          criteria.addSelectColumn(COMPANY_NAME);
          criteria.addSelectColumn(COMPANY_NAME_ASCII);
          criteria.addSelectColumn(COMPANY_UIC_CODE);
          criteria.addSelectColumn(COMPANY_URL);
          criteria.addSelectColumn(START_VALIDITY);
          criteria.addSelectColumn(END_VALIDITY);
          criteria.addSelectColumn(COMPANY_SHORT_NAME);
          criteria.addSelectColumn(FREE_TEXT);
          criteria.addSelectColumn(FREIGHT_FLAG);
          criteria.addSelectColumn(PASSENGER_FLAG);
          criteria.addSelectColumn(INFRASTRUCTURE_FLAG);
          criteria.addSelectColumn(OTHER_COMPANY_FLAG);
          criteria.addSelectColumn(NE_ENTITY_FLAG);
          criteria.addSelectColumn(CE_ENTITY_FLAG);
          criteria.addSelectColumn(CONTACT_PERSON);
          criteria.addSelectColumn(EMAIL);
          criteria.addSelectColumn(PHONE_NUMBER);
          criteria.addSelectColumn(FAX_NUMBER);
          criteria.addSelectColumn(ADDRESS);
          criteria.addSelectColumn(CITY);
          criteria.addSelectColumn(MOBILE_NUMBER);
          criteria.addSelectColumn(POSTAL_CODE);
          criteria.addSelectColumn(ACTIVE_FLAG);
      }

    /**
     * changes the boolean values in the criteria to the appropriate type,
     * whenever a booleanchar or booleanint column is involved.
     * This enables the user to create criteria using Boolean values
     * for booleanchar or booleanint columns
     * @param criteria the criteria in which the boolean values should be corrected
     * @throws TorqueException if the database map for the criteria cannot be 
               obtained.
     */
    public static void correctBooleans(Criteria criteria) throws TorqueException
    {
        correctBooleans(criteria, getTableMap());
    }

    /**
     * Create a new object of type cls from a resultset row starting
     * from a specified offset.  This is done so that you can select
     * other rows than just those needed for this object.  You may
     * for example want to create two objects from the same row.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static TCompany row2Object(Record row,
                                             int offset,
                                             Class cls)
        throws TorqueException
    {
        try
        {
            TCompany obj = (TCompany) cls.newInstance();
            TCompanyPeer.populateObject(row, offset, obj);
                  obj.setModified(false);
              obj.setNew(false);

            return obj;
        }
        catch (InstantiationException e)
        {
            throw new TorqueException(e);
        }
        catch (IllegalAccessException e)
        {
            throw new TorqueException(e);
        }
    }

    /**
     * Populates an object from a resultset row starting
     * from a specified offset.  This is done so that you can select
     * other rows than just those needed for this object.  You may
     * for example want to create two objects from the same row.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void populateObject(Record row,
                                      int offset,
                                      TCompany obj)
        throws TorqueException
    {
        try
        {
                obj.setHistId(row.getValue(offset + 0).asIntegerObj());
                  obj.setPlatnostOd(row.getValue(offset + 1).asUtilDate());
                  obj.setPlatnostDo(row.getValue(offset + 2).asUtilDate());
                  obj.setCasVytvorenia(row.getValue(offset + 3).asUtilDate());
                  obj.setCasZmeny(row.getValue(offset + 4).asUtilDate());
                  obj.setIdZmena(row.getValue(offset + 5).asIntegerObj());
                  obj.setZmaz(row.getValue(offset + 6).asString());
                  obj.setCompanyId(row.getValue(offset + 7).asIntegerObj());
                  obj.setIdCountry(row.getValue(offset + 8).asIntegerObj());
                  obj.setCompanyName(row.getValue(offset + 9).asString());
                  obj.setCompanyNameAscii(row.getValue(offset + 10).asString());
                  obj.setCompanyUicCode(row.getValue(offset + 11).asString());
                  obj.setCompanyUrl(row.getValue(offset + 12).asString());
                  obj.setStartValidity(row.getValue(offset + 13).asUtilDate());
                  obj.setEndValidity(row.getValue(offset + 14).asUtilDate());
                  obj.setCompanyShortName(row.getValue(offset + 15).asString());
                  obj.setFreeText(row.getValue(offset + 16).asString());
                  obj.setFreightFlag(row.getValue(offset + 17).asString());
                  obj.setPassengerFlag(row.getValue(offset + 18).asString());
                  obj.setInfrastructureFlag(row.getValue(offset + 19).asString());
                  obj.setOtherCompanyFlag(row.getValue(offset + 20).asString());
                  obj.setNeEntityFlag(row.getValue(offset + 21).asString());
                  obj.setCeEntityFlag(row.getValue(offset + 22).asString());
                  obj.setContactPerson(row.getValue(offset + 23).asString());
                  obj.setEmail(row.getValue(offset + 24).asString());
                  obj.setPhoneNumber(row.getValue(offset + 25).asString());
                  obj.setFaxNumber(row.getValue(offset + 26).asString());
                  obj.setAddress(row.getValue(offset + 27).asString());
                  obj.setCity(row.getValue(offset + 28).asString());
                  obj.setMobileNumber(row.getValue(offset + 29).asString());
                  obj.setPostalCode(row.getValue(offset + 30).asString());
                  obj.setActiveFlag(row.getValue(offset + 31).asString());
              }
        catch (DataSetException e)
        {
            throw new TorqueException(e);
        }
    }

    /**
     * Method to do selects.
     *
     * @param criteria object used to create the SELECT statement.
     * @return List of selected Objects
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static List doSelect(Criteria criteria) throws TorqueException
    {
        return populateObjects(doSelectVillageRecords(criteria));
    }

    /**
     * Method to do selects within a transaction.
     *
     * @param criteria object used to create the SELECT statement.
     * @param con the connection to use
     * @return List of selected Objects
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static List doSelect(Criteria criteria, Connection con)
        throws TorqueException
    {
        return populateObjects(doSelectVillageRecords(criteria, con));
    }

    /**
     * Grabs the raw Village records to be formed into objects.
     * This method handles connections internally.  The Record objects
     * returned by this method should be considered readonly.  Do not
     * alter the data and call save(), your results may vary, but are
     * certainly likely to result in hard to track MT bugs.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static List doSelectVillageRecords(Criteria criteria)
        throws TorqueException
    {
        return BaseTCompanyPeer
            .doSelectVillageRecords(criteria, (Connection) null);
    }

    /**
     * Grabs the raw Village records to be formed into objects.
     * This method should be used for transactions
     *
     * @param criteria object used to create the SELECT statement.
     * @param con the connection to use
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static List doSelectVillageRecords(Criteria criteria, Connection con)
        throws TorqueException
    {
        if (criteria.getSelectColumns().size() == 0)
        {
            addSelectColumns(criteria);
        }
        correctBooleans(criteria);

        setDbName(criteria);

        // BasePeer returns a List of Value (Village) arrays.  The array
        // order follows the order columns were placed in the Select clause.
        if (con == null)
        {
            return BasePeer.doSelect(criteria);
        }
        else
        {
            return BasePeer.doSelect(criteria, con);
        }
    }

    /**
     * The returned List will contain objects of the default type or
     * objects that inherit from the default.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static List populateObjects(List records)
        throws TorqueException
    {
        List results = new ArrayList(records.size());

        // populate the object(s)
        for (int i = 0; i < records.size(); i++)
        {
            Record row = (Record) records.get(i);
              results.add(TCompanyPeer.row2Object(row, 1,
                TCompanyPeer.getOMClass()));
          }
        return results;
    }
 

    /**
     * The class that the Peer will make instances of.
     * If the BO is abstract then you must implement this method
     * in the BO.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static Class getOMClass()
        throws TorqueException
    {
        return CLASS_DEFAULT;
    }

    /**
     * Method to do updates.
     *
     * @param criteria object containing data that is used to create the UPDATE
     *        statement.
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void doUpdate(Criteria criteria) throws TorqueException
    {
         BaseTCompanyPeer
            .doUpdate(criteria, (Connection) null);
    }

    /**
     * Method to do updates.  This method is to be used during a transaction,
     * otherwise use the doUpdate(Criteria) method.  It will take care of
     * the connection details internally.
     *
     * @param criteria object containing data that is used to create the UPDATE
     *        statement.
     * @param con the connection to use
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void doUpdate(Criteria criteria, Connection con)
        throws TorqueException
    {
        Criteria selectCriteria = new Criteria(DATABASE_NAME, 2);
        correctBooleans(criteria);

  
             selectCriteria.put(HIST_ID, criteria.remove(HIST_ID));
      
      
      
      
      
      
      
      
      
      
      
      
      
      
      
      
      
      
      
      
      
      
      
      
      
      
      
      
      
      
      
      
        setDbName(criteria);

        if (con == null)
        {
            BasePeer.doUpdate(selectCriteria, criteria);
        }
        else
        {
            BasePeer.doUpdate(selectCriteria, criteria, con);
        }
    }

    /**
     * Method to do deletes.
     *
     * @param criteria object containing data that is used DELETE from database.
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
     public static void doDelete(Criteria criteria) throws TorqueException
     {
         TCompanyPeer
            .doDelete(criteria, (Connection) null);
     }

    /**
     * Method to do deletes.  This method is to be used during a transaction,
     * otherwise use the doDelete(Criteria) method.  It will take care of
     * the connection details internally.
     *
     * @param criteria object containing data that is used DELETE from database.
     * @param con the connection to use
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
     public static void doDelete(Criteria criteria, Connection con)
        throws TorqueException
     {
        correctBooleans(criteria);

        setDbName(criteria);

        if (con == null)
        {
            BasePeer.doDelete(criteria);
        }
        else
        {
            BasePeer.doDelete(criteria, con);
        }
     }

    /**
     * Method to do selects
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static List doSelect(TCompany obj) throws TorqueException
    {
        return doSelect(buildSelectCriteria(obj));
    }

    /**
     * Method to do inserts
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void doInsert(TCompany obj) throws TorqueException
    {
          obj.setPrimaryKey(doInsert(buildCriteria(obj)));
          obj.setNew(false);
        obj.setModified(false);
    }

    /**
     * @param obj the data object to update in the database.
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void doUpdate(TCompany obj) throws TorqueException
    {
        doUpdate(buildCriteria(obj));
        obj.setModified(false);
    }

    /**
     * @param obj the data object to delete in the database.
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void doDelete(TCompany obj) throws TorqueException
    {
        doDelete(buildSelectCriteria(obj));
    }

    /**
     * Method to do inserts.  This method is to be used during a transaction,
     * otherwise use the doInsert(TCompany) method.  It will take
     * care of the connection details internally.
     *
     * @param obj the data object to insert into the database.
     * @param con the connection to use
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void doInsert(TCompany obj, Connection con)
        throws TorqueException
    {
          obj.setPrimaryKey(doInsert(buildCriteria(obj), con));
          obj.setNew(false);
        obj.setModified(false);
    }

    /**
     * Method to do update.  This method is to be used during a transaction,
     * otherwise use the doUpdate(TCompany) method.  It will take
     * care of the connection details internally.
     *
     * @param obj the data object to update in the database.
     * @param con the connection to use
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void doUpdate(TCompany obj, Connection con)
        throws TorqueException
    {
        doUpdate(buildCriteria(obj), con);
        obj.setModified(false);
    }

    /**
     * Method to delete.  This method is to be used during a transaction,
     * otherwise use the doDelete(TCompany) method.  It will take
     * care of the connection details internally.
     *
     * @param obj the data object to delete in the database.
     * @param con the connection to use
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void doDelete(TCompany obj, Connection con)
        throws TorqueException
    {
        doDelete(buildSelectCriteria(obj), con);
    }

    /**
     * Method to do deletes.
     *
     * @param pk ObjectKey that is used DELETE from database.
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void doDelete(ObjectKey pk) throws TorqueException
    {
        BaseTCompanyPeer
           .doDelete(pk, (Connection) null);
    }

    /**
     * Method to delete.  This method is to be used during a transaction,
     * otherwise use the doDelete(ObjectKey) method.  It will take
     * care of the connection details internally.
     *
     * @param pk the primary key for the object to delete in the database.
     * @param con the connection to use
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static void doDelete(ObjectKey pk, Connection con)
        throws TorqueException
    {
        doDelete(buildCriteria(pk), con);
    }

    /** Build a Criteria object from an ObjectKey */
    public static Criteria buildCriteria( ObjectKey pk )
    {
        Criteria criteria = new Criteria();
              criteria.add(HIST_ID, pk);
          return criteria;
     }

    /** Build a Criteria object from the data object for this peer */
    public static Criteria buildCriteria( TCompany obj )
    {
        Criteria criteria = new Criteria(DATABASE_NAME);
              if (!obj.isNew())
            criteria.add(HIST_ID, obj.getHistId());
              criteria.add(PLATNOST_OD, obj.getPlatnostOd());
              criteria.add(PLATNOST_DO, obj.getPlatnostDo());
              criteria.add(CAS_VYTVORENIA, obj.getCasVytvorenia());
              criteria.add(CAS_ZMENY, obj.getCasZmeny());
              criteria.add(ID_ZMENA, obj.getIdZmena());
              criteria.add(ZMAZ, obj.getZmaz());
              criteria.add(COMPANY_ID, obj.getCompanyId());
              criteria.add(ID_COUNTRY, obj.getIdCountry());
              criteria.add(COMPANY_NAME, obj.getCompanyName());
              criteria.add(COMPANY_NAME_ASCII, obj.getCompanyNameAscii());
              criteria.add(COMPANY_UIC_CODE, obj.getCompanyUicCode());
              criteria.add(COMPANY_URL, obj.getCompanyUrl());
              criteria.add(START_VALIDITY, obj.getStartValidity());
              criteria.add(END_VALIDITY, obj.getEndValidity());
              criteria.add(COMPANY_SHORT_NAME, obj.getCompanyShortName());
              criteria.add(FREE_TEXT, obj.getFreeText());
              criteria.add(FREIGHT_FLAG, obj.getFreightFlag());
              criteria.add(PASSENGER_FLAG, obj.getPassengerFlag());
              criteria.add(INFRASTRUCTURE_FLAG, obj.getInfrastructureFlag());
              criteria.add(OTHER_COMPANY_FLAG, obj.getOtherCompanyFlag());
              criteria.add(NE_ENTITY_FLAG, obj.getNeEntityFlag());
              criteria.add(CE_ENTITY_FLAG, obj.getCeEntityFlag());
              criteria.add(CONTACT_PERSON, obj.getContactPerson());
              criteria.add(EMAIL, obj.getEmail());
              criteria.add(PHONE_NUMBER, obj.getPhoneNumber());
              criteria.add(FAX_NUMBER, obj.getFaxNumber());
              criteria.add(ADDRESS, obj.getAddress());
              criteria.add(CITY, obj.getCity());
              criteria.add(MOBILE_NUMBER, obj.getMobileNumber());
              criteria.add(POSTAL_CODE, obj.getPostalCode());
              criteria.add(ACTIVE_FLAG, obj.getActiveFlag());
          return criteria;
    }

    /** Build a Criteria object from the data object for this peer, skipping all binary columns */
    public static Criteria buildSelectCriteria( TCompany obj )
    {
        Criteria criteria = new Criteria(DATABASE_NAME);
              if (!obj.isNew())
        {
                    criteria.add(HIST_ID, obj.getHistId());
                }
                          criteria.add(PLATNOST_OD, obj.getPlatnostOd());
                              criteria.add(PLATNOST_DO, obj.getPlatnostDo());
                              criteria.add(CAS_VYTVORENIA, obj.getCasVytvorenia());
                              criteria.add(CAS_ZMENY, obj.getCasZmeny());
                              criteria.add(ID_ZMENA, obj.getIdZmena());
                              criteria.add(ZMAZ, obj.getZmaz());
                              criteria.add(COMPANY_ID, obj.getCompanyId());
                              criteria.add(ID_COUNTRY, obj.getIdCountry());
                              criteria.add(COMPANY_NAME, obj.getCompanyName());
                              criteria.add(COMPANY_NAME_ASCII, obj.getCompanyNameAscii());
                              criteria.add(COMPANY_UIC_CODE, obj.getCompanyUicCode());
                              criteria.add(COMPANY_URL, obj.getCompanyUrl());
                              criteria.add(START_VALIDITY, obj.getStartValidity());
                              criteria.add(END_VALIDITY, obj.getEndValidity());
                              criteria.add(COMPANY_SHORT_NAME, obj.getCompanyShortName());
                              criteria.add(FREE_TEXT, obj.getFreeText());
                              criteria.add(FREIGHT_FLAG, obj.getFreightFlag());
                              criteria.add(PASSENGER_FLAG, obj.getPassengerFlag());
                              criteria.add(INFRASTRUCTURE_FLAG, obj.getInfrastructureFlag());
                              criteria.add(OTHER_COMPANY_FLAG, obj.getOtherCompanyFlag());
                              criteria.add(NE_ENTITY_FLAG, obj.getNeEntityFlag());
                              criteria.add(CE_ENTITY_FLAG, obj.getCeEntityFlag());
                              criteria.add(CONTACT_PERSON, obj.getContactPerson());
                              criteria.add(EMAIL, obj.getEmail());
                              criteria.add(PHONE_NUMBER, obj.getPhoneNumber());
                              criteria.add(FAX_NUMBER, obj.getFaxNumber());
                              criteria.add(ADDRESS, obj.getAddress());
                              criteria.add(CITY, obj.getCity());
                              criteria.add(MOBILE_NUMBER, obj.getMobileNumber());
                              criteria.add(POSTAL_CODE, obj.getPostalCode());
                              criteria.add(ACTIVE_FLAG, obj.getActiveFlag());
                  return criteria;
    }
 
    
        /**
     * Retrieve a single object by pk
     *
     * @param pk the primary key
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     * @throws NoRowsException Primary key was not found in database.
     * @throws TooManyRowsException Primary key was not found in database.
     */
    public static TCompany retrieveByPK(Integer pk)
        throws TorqueException, NoRowsException, TooManyRowsException
    {
        return retrieveByPK(SimpleKey.keyFor(pk));
    }

    /**
     * Retrieve a single object by pk
     *
     * @param pk the primary key
     * @param con the connection to use
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     * @throws NoRowsException Primary key was not found in database.
     * @throws TooManyRowsException Primary key was not found in database.
     */
    public static TCompany retrieveByPK(Integer pk, Connection con)
        throws TorqueException, NoRowsException, TooManyRowsException
    {
        return retrieveByPK(SimpleKey.keyFor(pk), con);
    }
  
    /**
     * Retrieve a single object by pk
     *
     * @param pk the primary key
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     * @throws NoRowsException Primary key was not found in database.
     * @throws TooManyRowsException Primary key was not found in database.
     */
    public static TCompany retrieveByPK(ObjectKey pk)
        throws TorqueException, NoRowsException, TooManyRowsException
    {
        Connection db = null;
        TCompany retVal = null;
        try
        {
            db = Torque.getConnection(DATABASE_NAME);
            retVal = retrieveByPK(pk, db);
        }
        finally
        {
            Torque.closeConnection(db);
        }
        return retVal;
    }

    /**
     * Retrieve a single object by pk
     *
     * @param pk the primary key
     * @param con the connection to use
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     * @throws NoRowsException Primary key was not found in database.
     * @throws TooManyRowsException Primary key was not found in database.
     */
    public static TCompany retrieveByPK(ObjectKey pk, Connection con)
        throws TorqueException, NoRowsException, TooManyRowsException
    {
        Criteria criteria = buildCriteria(pk);
        List v = doSelect(criteria, con);
        if (v.size() == 0)
        {
            throw new NoRowsException("Failed to select a row.");
        }
        else if (v.size() > 1)
        {
            throw new TooManyRowsException("Failed to select only one row.");
        }
        else
        {
            return (TCompany)v.get(0);
        }
    }

    /**
     * Retrieve a multiple objects by pk
     *
     * @param pks List of primary keys
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static List retrieveByPKs(List pks)
        throws TorqueException
    {
        Connection db = null;
        List retVal = null;
        try
        {
           db = Torque.getConnection(DATABASE_NAME);
           retVal = retrieveByPKs(pks, db);
        }
        finally
        {
            Torque.closeConnection(db);
        }
        return retVal;
    }

    /**
     * Retrieve a multiple objects by pk
     *
     * @param pks List of primary keys
     * @param dbcon the connection to use
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static List retrieveByPKs( List pks, Connection dbcon )
        throws TorqueException
    {
        List objs = null;
        if (pks == null || pks.size() == 0)
        {
            objs = new LinkedList();
        }
        else
        {
            Criteria criteria = new Criteria();
              criteria.addIn( HIST_ID, pks );
          objs = doSelect(criteria, dbcon);
        }
        return objs;
    }

 



        
  
  
    
  
      /**
     * Returns the TableMap related to this peer.
     *
     * @throws TorqueException Any exceptions caught during processing will be
     *         rethrown wrapped into a TorqueException.
     */
    public static TableMap getTableMap()
        throws TorqueException
    {
        return Torque.getDatabaseMap(DATABASE_NAME).getTable(TABLE_NAME);
    }
   
    private static void setDbName(Criteria crit)
    {
        // Set the correct dbName if it has not been overridden
        // crit.getDbName will return the same object if not set to
        // another value so == check is okay and faster
        if (crit.getDbName() == Torque.getDefaultDB())
        {
            crit.setDbName(DATABASE_NAME);
        }
    }
    

}
