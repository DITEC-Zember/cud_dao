package sk.ditec.dao.meta;

import java.sql.Connection;
import java.sql.SQLException;
import java.util.ArrayList;
import java.util.LinkedList;
import java.util.List;

import org.apache.torque.NoRowsException;
import org.apache.torque.TooManyRowsException;
import org.apache.torque.Torque;
import org.apache.torque.TorqueException;
import org.apache.torque.map.MapBuilder;
import org.apache.torque.map.TableMap;
import org.apache.torque.om.ObjectKey;
import org.apache.torque.om.SimpleKey;
import org.apache.torque.util.BasePeer;
import org.apache.torque.util.Criteria;

import sk.ditec.dao.meta.map.CudWfDefMapBuilder;

import com.workingdogs.village.DataSetException;
import com.workingdogs.village.QueryDataSet;
import com.workingdogs.village.Record;

/**
 * This class was autogenerated by Torque on:
 * 
 * [Mon Oct 20 12:40:10 CEST 2025]
 * 
 */
public abstract class BaseCudWfDefPeer extends BasePeer {
	/** Serial version */
	private static final long serialVersionUID = 1760956810281L;

	/** the default database name for this class */
	public static final String DATABASE_NAME;

	/** the table name for this class */
	public static final String TABLE_NAME;

	/**
	 * @return the map builder for this peer
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static MapBuilder getMapBuilder() throws TorqueException {
		return getMapBuilder(CudWfDefMapBuilder.CLASS_NAME);
	}

	/** the column name for the WF_DEF_ID field */
	public static final String WF_DEF_ID;
	/** the column name for the ID_CISELNIK field */
	public static final String ID_CISELNIK;
	/** the column name for the ID_WF_DEF_NASL field */
	public static final String ID_WF_DEF_NASL;
	/** the column name for the NAZOV field */
	public static final String NAZOV;
	/** the column name for the TYP field */
	public static final String TYP;
	/** the column name for the ZODPOVEDNOST field */
	public static final String ZODPOVEDNOST;
	/** the column name for the EMAIL_LIST field */
	public static final String EMAIL_LIST;
	/** the column name for the EMAIL_TEXT field */
	public static final String EMAIL_TEXT;
	/** the column name for the EMAIL_SUBJECT field */
	public static final String EMAIL_SUBJECT;
	/** the column name for the EMAIL_SEND field */
	public static final String EMAIL_SEND;
	/** the column name for the HODINY field */
	public static final String HODINY;
	/** the column name for the ID_SKUPINA field */
	public static final String ID_SKUPINA;
	/** the column name for the SKUPINA_NAZOV field */
	public static final String SKUPINA_NAZOV;
	/** the column name for the CAS_ZMENY field */
	public static final String CAS_ZMENY;
	/** the column name for the ID_UCET field */
	public static final String ID_UCET;
	/** the column name for the ID_TRANSAKCIA_ZAPISANE field */
	public static final String ID_TRANSAKCIA_ZAPISANE;
	/** the column name for the ID_TRANSAKCIA_ZRUSENE field */
	public static final String ID_TRANSAKCIA_ZRUSENE;

	static {
		DATABASE_NAME = "APP";
		TABLE_NAME = "CUD_WF_DEF";

		WF_DEF_ID = "CUD_WF_DEF.WF_DEF_ID";
		ID_CISELNIK = "CUD_WF_DEF.ID_CISELNIK";
		ID_WF_DEF_NASL = "CUD_WF_DEF.ID_WF_DEF_NASL";
		NAZOV = "CUD_WF_DEF.NAZOV";
		TYP = "CUD_WF_DEF.TYP";
		ZODPOVEDNOST = "CUD_WF_DEF.ZODPOVEDNOST";
		EMAIL_LIST = "CUD_WF_DEF.EMAIL_LIST";
		EMAIL_TEXT = "CUD_WF_DEF.EMAIL_TEXT";
		EMAIL_SUBJECT = "CUD_WF_DEF.EMAIL_SUBJECT";
		EMAIL_SEND = "CUD_WF_DEF.EMAIL_SEND";
		HODINY = "CUD_WF_DEF.HODINY";
		ID_SKUPINA = "CUD_WF_DEF.ID_SKUPINA";
		SKUPINA_NAZOV = "CUD_WF_DEF.SKUPINA_NAZOV";
		CAS_ZMENY = "CUD_WF_DEF.CAS_ZMENY";
		ID_UCET = "CUD_WF_DEF.ID_UCET";
		ID_TRANSAKCIA_ZAPISANE = "CUD_WF_DEF.ID_TRANSAKCIA_ZAPISANE";
		ID_TRANSAKCIA_ZRUSENE = "CUD_WF_DEF.ID_TRANSAKCIA_ZRUSENE";
		if (Torque.isInit()) {
			try {
				getMapBuilder(CudWfDefMapBuilder.CLASS_NAME);
			} catch (Exception e) {
				log.error("Could not initialize Peer", e);
				throw new RuntimeException(e);
			}
		} else {
			Torque.registerMapBuilder(CudWfDefMapBuilder.CLASS_NAME);
		}
	}

	/** number of columns for this peer */
	public static final int numColumns = 17;

	/** A class that can be returned by this peer. */
	protected static final String CLASSNAME_DEFAULT = "sk.ditec.dao.meta.CudWfDef";

	/** A class that can be returned by this peer. */
	protected static final Class CLASS_DEFAULT = initClass(CLASSNAME_DEFAULT);

	/**
	 * Class object initialization method.
	 * 
	 * @param className
	 *            name of the class to initialize
	 * @return the initialized class
	 */
	private static Class initClass(String className) {
		Class c = null;
		try {
			c = Class.forName(className);
		} catch (Throwable t) {
			log.error("A FATAL ERROR has occurred which should not " + "have happened under any circumstance.  Please notify " + "the Torque developers <torque-dev@db.apache.org> " + "and give as many details as possible (including the error " + "stack trace).", t);

			// Error objects should always be propogated.
			if (t instanceof Error) {
				throw (Error) t.fillInStackTrace();
			}
		}
		return c;
	}

	/**
	 * Get the list of objects for a ResultSet. Please not that your resultset MUST return columns in the right order. You can use getFieldNames() in BaseObject to get the correct
	 * sequence.
	 * 
	 * @param results
	 *            the ResultSet
	 * @return the list of objects
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static List resultSet2Objects(java.sql.ResultSet results) throws TorqueException {
		try {
			QueryDataSet qds = null;
			List rows = null;
			try {
				qds = new QueryDataSet(results);
				rows = getSelectResults(qds);
			} finally {
				if (qds != null) {
					qds.close();
				}
			}

			return populateObjects(rows);
		} catch (SQLException e) {
			throw new TorqueException(e);
		} catch (DataSetException e) {
			throw new TorqueException(e);
		}
	}

	/**
	 * Method to do inserts.
	 * 
	 * @param criteria
	 *            object used to create the INSERT statement.
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static ObjectKey doInsert(Criteria criteria) throws TorqueException {
		return BaseCudWfDefPeer.doInsert(criteria, (Connection) null);
	}

	/**
	 * Method to do inserts. This method is to be used during a transaction, otherwise use the doInsert(Criteria) method. It will take care of the connection details internally.
	 * 
	 * @param criteria
	 *            object used to create the INSERT statement.
	 * @param con
	 *            the connection to use
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static ObjectKey doInsert(Criteria criteria, Connection con) throws TorqueException {
		correctBooleans(criteria);

		setDbName(criteria);

		if (con == null) {
			return BasePeer.doInsert(criteria);
		} else {
			return BasePeer.doInsert(criteria, con);
		}
	}

	/**
	 * Add all the columns needed to create a new object.
	 * 
	 * @param criteria
	 *            object containing the columns to add.
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static void addSelectColumns(Criteria criteria) throws TorqueException {
		criteria.addSelectColumn(WF_DEF_ID);
		criteria.addSelectColumn(ID_CISELNIK);
		criteria.addSelectColumn(ID_WF_DEF_NASL);
		criteria.addSelectColumn(NAZOV);
		criteria.addSelectColumn(TYP);
		criteria.addSelectColumn(ZODPOVEDNOST);
		criteria.addSelectColumn(EMAIL_LIST);
		criteria.addSelectColumn(EMAIL_TEXT);
		criteria.addSelectColumn(EMAIL_SUBJECT);
		criteria.addSelectColumn(EMAIL_SEND);
		criteria.addSelectColumn(HODINY);
		criteria.addSelectColumn(ID_SKUPINA);
		criteria.addSelectColumn(SKUPINA_NAZOV);
		criteria.addSelectColumn(CAS_ZMENY);
		criteria.addSelectColumn(ID_UCET);
		criteria.addSelectColumn(ID_TRANSAKCIA_ZAPISANE);
		criteria.addSelectColumn(ID_TRANSAKCIA_ZRUSENE);
	}

	/**
	 * changes the boolean values in the criteria to the appropriate type, whenever a booleanchar or booleanint column is involved. This enables the user to create criteria using
	 * Boolean values for booleanchar or booleanint columns
	 * 
	 * @param criteria
	 *            the criteria in which the boolean values should be corrected
	 * @throws TorqueException
	 *             if the database map for the criteria cannot be obtained.
	 */
	public static void correctBooleans(Criteria criteria) throws TorqueException {
		correctBooleans(criteria, getTableMap());
	}

	/**
	 * Create a new object of type cls from a resultset row starting from a specified offset. This is done so that you can select other rows than just those needed for this object.
	 * You may for example want to create two objects from the same row.
	 * 
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static CudWfDef row2Object(Record row, int offset, Class cls) throws TorqueException {
		try {
			CudWfDef obj = (CudWfDef) cls.newInstance();
			CudWfDefPeer.populateObject(row, offset, obj);
			obj.setModified(false);
			obj.setNew(false);

			return obj;
		} catch (InstantiationException e) {
			throw new TorqueException(e);
		} catch (IllegalAccessException e) {
			throw new TorqueException(e);
		}
	}

	/**
	 * Populates an object from a resultset row starting from a specified offset. This is done so that you can select other rows than just those needed for this object. You may for
	 * example want to create two objects from the same row.
	 * 
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static void populateObject(Record row, int offset, CudWfDef obj) throws TorqueException {
		try {
			obj.setWfDefId(row.getValue(offset + 0).asIntegerObj());
			obj.setIdCiselnik(row.getValue(offset + 1).asIntegerObj());
			obj.setIdWfDefNasl(row.getValue(offset + 2).asIntegerObj());
			obj.setNazov(row.getValue(offset + 3).asString());
			obj.setTyp(row.getValue(offset + 4).asString());
			obj.setZodpovednost(row.getValue(offset + 5).asString());
			obj.setEmailList(row.getValue(offset + 6).asString());
			obj.setEmailText(row.getValue(offset + 7).asString());
			obj.setEmailSubject(row.getValue(offset + 8).asString());
			obj.setEmailSend(row.getValue(offset + 9).asString());
			obj.setHodiny(row.getValue(offset + 10).asIntegerObj());
			obj.setIdSkupina(row.getValue(offset + 11).asIntegerObj());
			obj.setSkupinaNazov(row.getValue(offset + 12).asString());
			obj.setCasZmeny(row.getValue(offset + 13).asUtilDate());
			obj.setIdUcet(row.getValue(offset + 14).asIntegerObj());
			obj.setIdTransakciaZapisane(row.getValue(offset + 15).asLongObj());
			obj.setIdTransakciaZrusene(row.getValue(offset + 16).asLongObj());
		} catch (DataSetException e) {
			throw new TorqueException(e);
		}
	}

	/**
	 * Method to do selects.
	 * 
	 * @param criteria
	 *            object used to create the SELECT statement.
	 * @return List of selected Objects
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static List doSelect(Criteria criteria) throws TorqueException {
		return populateObjects(doSelectVillageRecords(criteria));
	}

	/**
	 * Method to do selects within a transaction.
	 * 
	 * @param criteria
	 *            object used to create the SELECT statement.
	 * @param con
	 *            the connection to use
	 * @return List of selected Objects
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static List doSelect(Criteria criteria, Connection con) throws TorqueException {
		return populateObjects(doSelectVillageRecords(criteria, con));
	}

	/**
	 * Grabs the raw Village records to be formed into objects. This method handles connections internally. The Record objects returned by this method should be considered
	 * readonly. Do not alter the data and call save(), your results may vary, but are certainly likely to result in hard to track MT bugs.
	 * 
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static List doSelectVillageRecords(Criteria criteria) throws TorqueException {
		return BaseCudWfDefPeer.doSelectVillageRecords(criteria, (Connection) null);
	}

	/**
	 * Grabs the raw Village records to be formed into objects. This method should be used for transactions
	 * 
	 * @param criteria
	 *            object used to create the SELECT statement.
	 * @param con
	 *            the connection to use
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static List doSelectVillageRecords(Criteria criteria, Connection con) throws TorqueException {
		if (criteria.getSelectColumns().size() == 0) {
			addSelectColumns(criteria);
		}
		correctBooleans(criteria);

		setDbName(criteria);

		// BasePeer returns a List of Value (Village) arrays. The array
		// order follows the order columns were placed in the Select clause.
		if (con == null) {
			return BasePeer.doSelect(criteria);
		} else {
			return BasePeer.doSelect(criteria, con);
		}
	}

	/**
	 * The returned List will contain objects of the default type or objects that inherit from the default.
	 * 
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static List populateObjects(List records) throws TorqueException {
		List results = new ArrayList(records.size());

		// populate the object(s)
		for (int i = 0; i < records.size(); i++) {
			Record row = (Record) records.get(i);
			results.add(CudWfDefPeer.row2Object(row, 1, CudWfDefPeer.getOMClass()));
		}
		return results;
	}

	/**
	 * The class that the Peer will make instances of. If the BO is abstract then you must implement this method in the BO.
	 * 
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static Class getOMClass() throws TorqueException {
		return CLASS_DEFAULT;
	}

	/**
	 * Method to do updates.
	 * 
	 * @param criteria
	 *            object containing data that is used to create the UPDATE statement.
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static void doUpdate(Criteria criteria) throws TorqueException {
		BaseCudWfDefPeer.doUpdate(criteria, (Connection) null);
	}

	/**
	 * Method to do updates. This method is to be used during a transaction, otherwise use the doUpdate(Criteria) method. It will take care of the connection details internally.
	 * 
	 * @param criteria
	 *            object containing data that is used to create the UPDATE statement.
	 * @param con
	 *            the connection to use
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static void doUpdate(Criteria criteria, Connection con) throws TorqueException {
		Criteria selectCriteria = new Criteria(DATABASE_NAME, 2);
		correctBooleans(criteria);

		selectCriteria.put(WF_DEF_ID, criteria.remove(WF_DEF_ID));

		setDbName(criteria);

		if (con == null) {
			BasePeer.doUpdate(selectCriteria, criteria);
		} else {
			BasePeer.doUpdate(selectCriteria, criteria, con);
		}
	}

	/**
	 * Method to do deletes.
	 * 
	 * @param criteria
	 *            object containing data that is used DELETE from database.
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static void doDelete(Criteria criteria) throws TorqueException {
		CudWfDefPeer.doDelete(criteria, (Connection) null);
	}

	/**
	 * Method to do deletes. This method is to be used during a transaction, otherwise use the doDelete(Criteria) method. It will take care of the connection details internally.
	 * 
	 * @param criteria
	 *            object containing data that is used DELETE from database.
	 * @param con
	 *            the connection to use
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static void doDelete(Criteria criteria, Connection con) throws TorqueException {
		correctBooleans(criteria);

		setDbName(criteria);

		if (con == null) {
			BasePeer.doDelete(criteria);
		} else {
			BasePeer.doDelete(criteria, con);
		}
	}

	/**
	 * Method to do selects
	 * 
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static List doSelect(CudWfDef obj) throws TorqueException {
		return doSelect(buildSelectCriteria(obj));
	}

	/**
	 * Method to do inserts
	 * 
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static void doInsert(CudWfDef obj) throws TorqueException {
		obj.setPrimaryKey(doInsert(buildCriteria(obj)));
		obj.setNew(false);
		obj.setModified(false);
	}

	/**
	 * @param obj
	 *            the data object to update in the database.
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static void doUpdate(CudWfDef obj) throws TorqueException {
		doUpdate(buildCriteria(obj));
		obj.setModified(false);
	}

	/**
	 * @param obj
	 *            the data object to delete in the database.
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static void doDelete(CudWfDef obj) throws TorqueException {
		doDelete(buildSelectCriteria(obj));
	}

	/**
	 * Method to do inserts. This method is to be used during a transaction, otherwise use the doInsert(CudWfDef) method. It will take care of the connection details internally.
	 * 
	 * @param obj
	 *            the data object to insert into the database.
	 * @param con
	 *            the connection to use
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static void doInsert(CudWfDef obj, Connection con) throws TorqueException {
		obj.setPrimaryKey(doInsert(buildCriteria(obj), con));
		obj.setNew(false);
		obj.setModified(false);
	}

	/**
	 * Method to do update. This method is to be used during a transaction, otherwise use the doUpdate(CudWfDef) method. It will take care of the connection details internally.
	 * 
	 * @param obj
	 *            the data object to update in the database.
	 * @param con
	 *            the connection to use
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static void doUpdate(CudWfDef obj, Connection con) throws TorqueException {
		doUpdate(buildCriteria(obj), con);
		obj.setModified(false);
	}

	/**
	 * Method to delete. This method is to be used during a transaction, otherwise use the doDelete(CudWfDef) method. It will take care of the connection details internally.
	 * 
	 * @param obj
	 *            the data object to delete in the database.
	 * @param con
	 *            the connection to use
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static void doDelete(CudWfDef obj, Connection con) throws TorqueException {
		doDelete(buildSelectCriteria(obj), con);
	}

	/**
	 * Method to do deletes.
	 * 
	 * @param pk
	 *            ObjectKey that is used DELETE from database.
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static void doDelete(ObjectKey pk) throws TorqueException {
		BaseCudWfDefPeer.doDelete(pk, (Connection) null);
	}

	/**
	 * Method to delete. This method is to be used during a transaction, otherwise use the doDelete(ObjectKey) method. It will take care of the connection details internally.
	 * 
	 * @param pk
	 *            the primary key for the object to delete in the database.
	 * @param con
	 *            the connection to use
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static void doDelete(ObjectKey pk, Connection con) throws TorqueException {
		doDelete(buildCriteria(pk), con);
	}

	/** Build a Criteria object from an ObjectKey */
	public static Criteria buildCriteria(ObjectKey pk) {
		Criteria criteria = new Criteria();
		criteria.add(WF_DEF_ID, pk);
		return criteria;
	}

	/** Build a Criteria object from the data object for this peer */
	public static Criteria buildCriteria(CudWfDef obj) {
		Criteria criteria = new Criteria(DATABASE_NAME);
		if (!obj.isNew())
			criteria.add(WF_DEF_ID, obj.getWfDefId());
		criteria.add(ID_CISELNIK, obj.getIdCiselnik());
		criteria.add(ID_WF_DEF_NASL, obj.getIdWfDefNasl());
		criteria.add(NAZOV, obj.getNazov());
		criteria.add(TYP, obj.getTyp());
		criteria.add(ZODPOVEDNOST, obj.getZodpovednost());
		criteria.add(EMAIL_LIST, obj.getEmailList());
		criteria.add(EMAIL_TEXT, obj.getEmailText());
		criteria.add(EMAIL_SUBJECT, obj.getEmailSubject());
		criteria.add(EMAIL_SEND, obj.getEmailSend());
		criteria.add(HODINY, obj.getHodiny());
		criteria.add(ID_SKUPINA, obj.getIdSkupina());
		criteria.add(SKUPINA_NAZOV, obj.getSkupinaNazov());
		criteria.add(CAS_ZMENY, obj.getCasZmeny());
		criteria.add(ID_UCET, obj.getIdUcet());
		criteria.add(ID_TRANSAKCIA_ZAPISANE, obj.getIdTransakciaZapisane());
		criteria.add(ID_TRANSAKCIA_ZRUSENE, obj.getIdTransakciaZrusene());
		return criteria;
	}

	/** Build a Criteria object from the data object for this peer, skipping all binary columns */
	public static Criteria buildSelectCriteria(CudWfDef obj) {
		Criteria criteria = new Criteria(DATABASE_NAME);
		if (!obj.isNew()) {
			criteria.add(WF_DEF_ID, obj.getWfDefId());
		}
		criteria.add(ID_CISELNIK, obj.getIdCiselnik());
		criteria.add(ID_WF_DEF_NASL, obj.getIdWfDefNasl());
		criteria.add(NAZOV, obj.getNazov());
		criteria.add(TYP, obj.getTyp());
		criteria.add(ZODPOVEDNOST, obj.getZodpovednost());
		criteria.add(EMAIL_LIST, obj.getEmailList());
		criteria.add(EMAIL_TEXT, obj.getEmailText());
		criteria.add(EMAIL_SUBJECT, obj.getEmailSubject());
		criteria.add(EMAIL_SEND, obj.getEmailSend());
		criteria.add(HODINY, obj.getHodiny());
		criteria.add(ID_SKUPINA, obj.getIdSkupina());
		criteria.add(SKUPINA_NAZOV, obj.getSkupinaNazov());
		criteria.add(CAS_ZMENY, obj.getCasZmeny());
		criteria.add(ID_UCET, obj.getIdUcet());
		criteria.add(ID_TRANSAKCIA_ZAPISANE, obj.getIdTransakciaZapisane());
		criteria.add(ID_TRANSAKCIA_ZRUSENE, obj.getIdTransakciaZrusene());
		return criteria;
	}

	/**
	 * Retrieve a single object by pk
	 * 
	 * @param pk
	 *            the primary key
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 * @throws NoRowsException
	 *             Primary key was not found in database.
	 * @throws TooManyRowsException
	 *             Primary key was not found in database.
	 */
	public static CudWfDef retrieveByPK(Integer pk) throws TorqueException, NoRowsException, TooManyRowsException {
		return retrieveByPK(SimpleKey.keyFor(pk));
	}

	/**
	 * Retrieve a single object by pk
	 * 
	 * @param pk
	 *            the primary key
	 * @param con
	 *            the connection to use
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 * @throws NoRowsException
	 *             Primary key was not found in database.
	 * @throws TooManyRowsException
	 *             Primary key was not found in database.
	 */
	public static CudWfDef retrieveByPK(Integer pk, Connection con) throws TorqueException, NoRowsException, TooManyRowsException {
		return retrieveByPK(SimpleKey.keyFor(pk), con);
	}

	/**
	 * Retrieve a single object by pk
	 * 
	 * @param pk
	 *            the primary key
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 * @throws NoRowsException
	 *             Primary key was not found in database.
	 * @throws TooManyRowsException
	 *             Primary key was not found in database.
	 */
	public static CudWfDef retrieveByPK(ObjectKey pk) throws TorqueException, NoRowsException, TooManyRowsException {
		Connection db = null;
		CudWfDef retVal = null;
		try {
			db = Torque.getConnection(DATABASE_NAME);
			retVal = retrieveByPK(pk, db);
		} finally {
			Torque.closeConnection(db);
		}
		return retVal;
	}

	/**
	 * Retrieve a single object by pk
	 * 
	 * @param pk
	 *            the primary key
	 * @param con
	 *            the connection to use
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 * @throws NoRowsException
	 *             Primary key was not found in database.
	 * @throws TooManyRowsException
	 *             Primary key was not found in database.
	 */
	public static CudWfDef retrieveByPK(ObjectKey pk, Connection con) throws TorqueException, NoRowsException, TooManyRowsException {
		Criteria criteria = buildCriteria(pk);
		List v = doSelect(criteria, con);
		if (v.size() == 0) {
			throw new NoRowsException("Failed to select a row.");
		} else if (v.size() > 1) {
			throw new TooManyRowsException("Failed to select only one row.");
		} else {
			return (CudWfDef) v.get(0);
		}
	}

	/**
	 * Retrieve a multiple objects by pk
	 * 
	 * @param pks
	 *            List of primary keys
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static List retrieveByPKs(List pks) throws TorqueException {
		Connection db = null;
		List retVal = null;
		try {
			db = Torque.getConnection(DATABASE_NAME);
			retVal = retrieveByPKs(pks, db);
		} finally {
			Torque.closeConnection(db);
		}
		return retVal;
	}

	/**
	 * Retrieve a multiple objects by pk
	 * 
	 * @param pks
	 *            List of primary keys
	 * @param dbcon
	 *            the connection to use
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static List retrieveByPKs(List pks, Connection dbcon) throws TorqueException {
		List objs = null;
		if (pks == null || pks.size() == 0) {
			objs = new LinkedList();
		} else {
			Criteria criteria = new Criteria();
			criteria.addIn(WF_DEF_ID, pks);
			objs = doSelect(criteria, dbcon);
		}
		return objs;
	}

	/**
	 * Returns the TableMap related to this peer.
	 * 
	 * @throws TorqueException
	 *             Any exceptions caught during processing will be rethrown wrapped into a TorqueException.
	 */
	public static TableMap getTableMap() throws TorqueException {
		return Torque.getDatabaseMap(DATABASE_NAME).getTable(TABLE_NAME);
	}

	private static void setDbName(Criteria crit) {
		// Set the correct dbName if it has not been overridden
		// crit.getDbName will return the same object if not set to
		// another value so == check is okay and faster
		if (crit.getDbName() == Torque.getDefaultDB()) {
			crit.setDbName(DATABASE_NAME);
		}
	}

}
